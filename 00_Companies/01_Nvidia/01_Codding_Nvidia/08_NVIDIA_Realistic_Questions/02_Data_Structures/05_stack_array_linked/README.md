# Implement stack using array + linked list

## In-depth Explanation (Nvidia Interview Style)

- Stack (LIFO) can be implemented using arrays (fixed size) or linked lists (dynamic size).
- Array: Use an index to track the top.
- Linked list: Push/pop at the head.

### Interview Tips
- Be ready to discuss trade-offs (memory, performance, overflow).

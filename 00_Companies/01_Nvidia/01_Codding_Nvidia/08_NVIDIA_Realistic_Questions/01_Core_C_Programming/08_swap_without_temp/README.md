# Swap two numbers without temp

## Code
See solution.c for the XOR swap implementation.

## In-depth Explanation (Nvidia Interview Style)

- XOR swap uses bitwise XOR to exchange values without a temporary variable.
- Not recommended for production (can be confusing, fails if a and b point to the same address).

### Interview Tips
- Be ready to discuss why XOR swap is rarely used in real code.

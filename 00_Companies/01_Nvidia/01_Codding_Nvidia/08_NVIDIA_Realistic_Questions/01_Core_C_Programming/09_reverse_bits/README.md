# Reverse bits of an integer

## Code
See solution.c for the implementation.

## In-depth Explanation (Nvidia Interview Style)

- Bit reversal is used in low-level code (e.g., CRC, FFT, hardware drivers).
- The loop shifts bits from input to output in reverse order.

### Interview Tips
- Be ready to discuss optimizations (lookup tables, __builtin_bitreverse).
